{
  "commitMessage": "Bumps to version v%s",
  "tagName": "v%s",
  "scripts": {
    "preupdate": "npm run clean && npm run build",
    "postcommit": "git push && git push --tags && npm publish",
    "postupdate": "echo 'New version v%s tagged and released'"
  }
}